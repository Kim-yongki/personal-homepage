{
  "name": "my-portfolio",
  "version": "1.0.0",
  "private": true,
  "homepage": "https://Kim-yongki.github.io/personal-homepage-template",
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "gh-pages": "^6.1.1",
    "esbuild": "^0.23.0"
  },
"scripts": {
  "start": "npx serve -s build -l 3000",
  "build": "if exist build rmdir /s /q build && mkdir build && npx esbuild src/index.js --bundle --outfile=build/assets/bundle.js --define:process.env.NODE_ENV='\"production\"' --minify --loader:.js=jsx && xcopy public build /E /I /Y",
  "predeploy": "npm run build",
  "deploy": "gh-pages -d build"
}}